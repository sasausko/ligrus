<div id="<%= dom_id article %>">
  <p>
    <strong>Name:</strong>
    <%= article.name %>
  </p>

  <p>
  <strong>Body:</strong>
    <%= article.body %>
  </p>

  <p>
  Tags:
  <% article.tags.each do |tag| %>
    <%= link_to tag.name, tag_path(tag) %>
  <% end %>
  </p>

</div>
